[project]
name = "sp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sralter", email = "s.r.alter@icloud.com" }
]
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.5.5",
    "geopandas>=1.1.4",
    "osmnx>=2.1.1",
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
    "pysal>=26.1",
]

[project.scripts]
sp = "sp:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
